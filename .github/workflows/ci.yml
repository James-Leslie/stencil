name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  python:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      # Installs and activates the tools pinned in mise.toml (python, node, prek, just),
      # so CI uses the same runtime definition as local development.
      - name: Set up mise
        uses: jdx/mise-action@v4

      # uv is machine-level locally (not in mise.toml), so CI installs it explicitly.
      - name: Install uv
        uses: astral-sh/setup-uv@v8.1.0
        with:
          enable-cache: true

      - name: Sync dependencies
        run: uv sync

      - name: Lint
        run: uv run ruff check .

      - name: Format check
        run: uv run ruff format --check .

      - name: Type check
        run: uv check

      - name: Test
        run: uv run pytest
